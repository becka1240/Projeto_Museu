<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Identificação</title>
</head>
<body>
    <%- include("../partials/cabecalho.ejs") %>
    <br><br>
    <div class="container">
    <% if ( obra ) { %>
        <div>
            <b>Identificação:</b> <%= obra.identificacao %>
        </div>
        <div>
            <b>Nome:</b> <%= obra.nome %>
        </div>
        <div>
            <b>Autor:</b> <%= obra.autor %>
        </div>
    <% } else { %>
        <h2>Obra não encontrada</h2>
    <% } %>
    <br>
    <a href="/obras" class="btn btn-success">Voltar</a>
    </div>
    <%- include("../partials/rodape.ejs") %>
 
</body>
</html>